using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DisplayValue : MonoBehaviour {

    public EggMovement eggMovement;

	// Use this for initialization
	void Start () {
        //How do I access the datatype located inside a class?
        //gameObject.transform.GetChild(0).GetComponent<Text>().text = eggMovement;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
